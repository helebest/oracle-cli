# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/

# Credentials - entire directory
credentials/

# Config with sensitive data (use config.example.yaml as template)
config.yaml

# Docker service secrets (compose .env files, rclone config)
docker/*/.env
docker/obsidian-sync/rclone.conf

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Logs
*.log

# Claude Code per-user state (keep shared .claude/settings.json if we ever add one)
.claude/settings.local.json
.claude/scheduled_tasks.lock
